{
  "description": "Create Angular Component",
  "files": [
    {
      "path": "<%= it.toKebabCase(it.name) %>/<%= it.toKebabCase(it.name) %>.component.ts",
      "template": "component.ts.eta"
    },
    {
      "path": "<%= it.toKebabCase(it.name) %>/<%= it.toKebabCase(it.name) %>.component.html",
      "template": "component.html.eta"
    },
    {
      "path": "<%= it.toKebabCase(it.name) %>/<%= it.toKebabCase(it.name) %>.component.scss",
      "template": "component.scss.eta"
    },
    {
      "path": "<%= it.toKebabCase(it.name) %>/<%= it.toKebabCase(it.name) %>.component.spec.ts",
      "template": "component.spec.ts.eta"
    }
  ],
  "context": {
    "input": {
      "name": {
        "type": "string",
        "description": "Component name (without the word 'Component')",
        "required": true
      }
    }
  }
}
